#include <stdio.h>
#include <stdlib.h>
int main()
{
    int n,k;
    int *x = (int *)malloc(sizeof(int)*20);
    scanf("%d %d",&n,&k);
    for(int i=0;i<n;i++)
    {
       scanf("%d",&n); 
    }
    
    free(x);
    return 0;
}

